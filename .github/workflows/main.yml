name: Security Scan
on: [pull_request]
jobs:
  security-scan:
    runs-on: [self-hosted, local-armis]
    permissions:
      contents: read
      actions: read
      security-events: write
    steps:
      - name: Scan Repository
        env:
          ARMIS_CLIENT_ID: ${{ secrets.ARMIS_CLIENT_ID }}
          ARMIS_CLIENT_SECRET: ${{ secrets.ARMIS_CLIENT_SECRET }}
        run: |
          /Users/sheadrick/Documents/Projects/armis-cli-main/bin/armis-cli-debug \
          scan repo . \
          --format sarif > results.sarif
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: results.sarif
